# گزارش کنترل کیفیت نسخهٔ ۰٫۶٫۰

## نتیجه‌های موفق

- ۴۷ از ۴۷ تست منبع پروژه اجرا و موفق شد.
- ۶۴ فایل TypeScript/TSX از نظر نحو با esbuild بررسی شد.
- گراف import برای ۶۰ فایل غیرتست bundle شد و خطایی نداشت.
- پنج ابزار جدید و حالت خالی صفحهٔ صندوق‌ها با رندر سمت سرور آزمایش شدند.
- هر هفت slug روش سرمایه‌گذاری محتوای قابل بارگذاری دارند.
- اسکن نویسهٔ خراب، URL بدشکل، ادعای بی‌ریسک بودن و markerهای merge بدون نتیجه بود.

## بررسی نهایی پس از Push

محیط ساخت این بسته وابستگی‌های کامل Next.js را در اختیار نداشت؛ بنابراین اجرای بومی `npm test`، `npm run typecheck` و `npm run build` در همین محیط ممکن نبود. فایل GitHub Actions اضافه شده است تا پس از Push، نصب واقعی وابستگی‌ها، تست و TypeScript را خودکار اجرا کند. build فقط وقتی اجرا می‌شود که Secret با نام `DATABASE_URL` در GitHub تنظیم شده باشد.

در کامپیوتر محلی، بعد از `npm install` و با حفظ `.env.local` فعلی، دستور زیر همین بررسی‌ها را پشت‌سرهم انجام می‌دهد:

```powershell
npm run check
```
