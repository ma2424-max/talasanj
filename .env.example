# رشتهٔ اتصال دیتابیس PostgreSQL (Neon) را اینجا بگذار.
# این فایل فقط یک نمونه است؛ فایل واقعی .env.local نام دارد و هرگز به گیت‌هاب نمی‌رود.
DATABASE_URL="postgresql://USER:PASSWORD@HOST.neon.tech/talasanj?sslmode=require"
