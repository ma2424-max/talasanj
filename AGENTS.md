# AGENTS.md — قانون‌نامهٔ پروژهٔ طلاسنج

این فایل قانون‌نامهٔ الزامی برای هر عامل هوش مصنوعی است که روی این مخزن کار می‌کند. قبل از هر کار، همین فایل را کامل بخوان و به آن پایبند بمان. اگر دستور کاربر با این فایل تعارض داشت، ابتدا تعارض را صریح گزارش بده.

## ۱. محصول چیست؟

طلاسنج (talasanj.org) مرجع مستقل و فارسی‌زبان برای مقایسهٔ پلتفرم‌های خرید طلای آنلاین در ایران است.

- هستهٔ ارزش: اعتماد از طریق دادهٔ منبع‌دار، تاریخ‌دار و شفاف.
- مخاطب: خریدار عادی طلا در ایران (غیرحرفه‌ای).
- ممنوعیت مطلق: ارائهٔ توصیهٔ سرمایه‌گذاری؛ سایت «مقایسه» می‌کند، «توصیه» نمی‌کند.

## ۲. استک قفل‌شده

- Next.js 15 (App Router) + TypeScript با strict فعال + Tailwind CSS v4 (راست‌به‌چپ)
- PostgreSQL + Drizzle ORM + Zod (از اسپرینت S1)
- محتوای راهنماها: MDX
- تست: Vitest (واحد) + Playwright (E2E)
- رندر: SSR/ISR؛ تعامل فقط به‌صورت Progressive Enhancement — صفحه بدون JavaScript باید کامل خوانا باشد.
- هیچ وابستگی جدیدی بدون دلیل روشن و تأیید صاحب محصول به package.json اضافه نکن.

## ۳. ده قانون غیرقابل‌مذاکره

1. هیچ دادهٔ واقعی‌ای (کارمزد، مجوز، قیمت، امتیاز) هرگز از حافظهٔ خودت نساز؛ فقط از seed/دیتابیس. دادهٔ نمایشی فقط با پلتفرم `example-platform` و برچسب واضح «دادهٔ نمایشی».
2. هر عدد مالی باید منبع و تاریخ داشته باشد؛ عدد بدون منبع = «نامشخص».
3. زبان رابط فارسی، راست‌به‌چپ، فونت Vazirmatn؛ اعداد مالی با tabular-nums (کلاس `tnum`).
4. رنگ‌ها فقط از توکن‌های دیزاین سیستم در globals.css: `bg-base` و `bg-surface` و `gold` و `cream`.
5. هر صفحه دقیقاً یک H1 دارد؛ canonical خودارجاعی؛ breadcrumbs دیداری + BreadcrumbList.
6. لینک خروجی به پلتفرم‌ها فقط از مسیر ‎`/go/`‎ عبور می‌کند؛ لینک مستقیم خارجی به پلتفرم در بدنه ممنوع.
7. نرخ مالیات ارزش افزوده و هر ثابت اقتصادی فقط از فایل پیکربندی (`assumptions.json`) خوانده می‌شود؛ هاردکد ممنوع.
8. هر فرمول مالی حداقل ۵ تست طلایی دارد؛ بدون تست، ابزار منتشر نمی‌شود.
9. واژگان ممنوعه (مثل «تضمین سود»، «بهترین سرمایه‌گذاری»، «خرید حتمی») هرگز در متن نمی‌آیند.
10. پس از هر تغییر: `npm run typecheck` باید سبز باشد و صفحه در مرورگر بدون خطا دیده شود.

## ۴. مرزهای معماری

- `app/` فقط رندر صفحات؛ منطق داده در `lib/` و `db/`؛ فرمول‌های مالی در `lib/calc/` (توابع خالص و تست‌پذیر).
- کامپوننت‌های مشترک در `components/`؛ استایل inline پراکنده ممنوع.
- هر صفحهٔ جدید باید در «سند ساختار صفحات» ثبت شده باشد؛ صفحهٔ بیرون از سند نساز.

## ۵. قواعد کد

- TypeScript strict؛ `any` ممنوع مگر با توضیح کوتاه.
- نام‌گذاری: مفاهیم دامنه فارسی در متن و کامنت، شناسه‌های کد انگلیسی.
- کامنت فقط برای «چرا»، نه «چه».

## ۶. Definition of Done (برای همهٔ کارها)

- [ ] صفحه در مرورگر بدون خطا دیده شد
- [ ] موبایل و دسکتاپ چک شد
- [ ] بدون JavaScript خوانا است
- [ ] `npm run typecheck` سبز است
- [ ] فایل PROGRESS.md به‌روز شد

## ۷. ممنوعیت‌های ویژهٔ عامل AI

- هرگز دادهٔ واقعی پلتفرم را حدس نزن، جعل نکن یا «تقریبی» بنویس.
- هرگز کتابخانه، رنگ، فونت یا الگوی جدید بیرون از اسناد مرجع معرفی نکن.
- هرگز سکشن یا صفحه‌ای بیرون از سند ساختار صفحات اختراع نکن؛ کمبود مشخصات را گزارش بده.
- هرگز رمز، کلید یا مقدار محرمانه را در کد، commit یا چت ننویس؛ محل آن‌ها فقط `.env.local` است.

## اسناد مرجع (در Notion، از صاحب محصول بخواه)

- «طلاسنج — سند جامع ساخت سایت، محتوا و سئو»
- «طلاسنج — ساختار کامل صفحات سایت»
