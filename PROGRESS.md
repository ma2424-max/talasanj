# PROGRESS.md — دفترچهٔ خاطرات پروژهٔ طلاسنج

قانون: آخر هر جلسهٔ کاری، عامل AI این فایل را به‌روز می‌کند؛ اول هر جلسه، این فایل خوانده می‌شود.

## وضعیت کلی

- فاز فعلی: ۲ (ساخت صفحه‌به‌صفحه)
- آخرین اسپرینت کامل‌شده: S6 (کامل — هر دو بخش)
- تاریخ آخرین به‌روزرسانی: ۱۴۰۵/۰۵/۲۴

## انجام‌شده

- [x] S0 — اسکلت Next.js 15 + TypeScript strict + Tailwind v4 راست‌به‌چپ + فونت Vazirmatn + ۴۰۴ سفارشی + robots.txt
- [x] S1 — مدل داده با Drizzle + PostgreSQL (Neon): ۷ جدول + seed اولیه + صفحهٔ تست `/dev/db-check`
- [x] S2 — دیزاین سیستم: توکن‌های کامل + ۱۴ کامپوننت + گالری `/dev/design` + `lib/format.ts`
- [x] S3 — قالب پروفایل پلتفرم `/platforms/[slug]/` با ۱۷ سکشن + مسیر خروجی `/go/[slug]`
- [x] S4 — موتور امتیازدهی `lib/scoring/` + ۷ تست طلایی + اتصال زنده به پروفایل
- [x] S5 — ابزار شاخص «محاسبه‌گر هزینهٔ واقعی» + هاب `/tools/` + ۶ تست طلایی + اتصال به سکشن ۶ پروفایل
- [x] S6 — دایرکتوری `/platforms/` (فیلتر GET + مرتب‌سازی + CompareTray تعاملی + noindex فیلترها) + صفحهٔ اصلی کامل ۱۰ سکشن + `/search/` (noindex) + جدول `click_logs` و لاگ `/go/` + صفحهٔ `/compare/[a]-vs-[b]/` (حکم اتمی داده‌محور، جدول کنارهم، هزینه در ۵ مبلغ مرجع، تفکیک محورها، CTA دوتایی) + شش صفحهٔ `/best/[criterion]/` با موتور `lib/rankings/` و ۶ تست طلایی

## در حال انجام

- (خالی)

## قدم بعدی (جلسهٔ بعد)

- S7 — لایهٔ محتوا: `/methods/` هاب + ۷ صفحهٔ روش + `/guides/` و `/glossary/` با محتوای MDX (نویسنده، تاریخ، TOC) — متن‌های اولیه از سمت صاحب محصول/AI با قواعد پرامپت مادر.

## خطاها و مسائل باز

- لینک‌های `/methods/` فعلاً ۴۰۴ می‌دهد؛ در S7 ساخته می‌شوند — طبیعی است.
- سینی مقایسه و صفحهٔ compare با فقط یک پلتفرم در دیتابیس معنای کامل ندارند؛ با اضافه‌شدن پلتفرم دوم به بعد درست می‌شوند.

## تصمیم‌های قفل‌شده

- استک: Next.js 15 + TypeScript strict + Tailwind v4 + PostgreSQL (Neon) + Drizzle + Zod + Vitest
- رنگ‌ها: ‎--bg-base ‎#0F1115 ، ‎--bg-surface ‎#171A21 ، ‎--gold ‎#C9A227 ، ‎--cream ‎#F5F1E6 ، ‎--positive ‎#4CAF7D ، ‎--negative ‎#E35D5D ، ‎--warning ‎#E0A93E ، ‎--muted ‎#9AA3B2
- فونت: Vazirmatn (خودمیزبان از طریق @fontsource)
- دادهٔ واقعی فقط با منبع و تاریخ؛ دادهٔ نمایشی فقط با example-platform
- اتصال دیتابیس فقط از طریق getDb() در db/index.ts و متغیر DATABASE_URL در .env.local (هرگز در گیت commit نمی‌شود)
- صفحات داخلی توسعه (noindex) زیر مسیر `/dev/` می‌آیند
- رندر صفحات داده‌دار: ISR با revalidate پیش‌فرض ۳۶۰۰ ثانیه
- هر فرمول مالی و هر موتور رتبه‌بندی بدون حداقل ۵ تست طلایی منتشر نمی‌شود (`npm test` باید سبز باشد)
- URLهای فیلتر/نتیجهٔ پارامتردار noindex و canonical به نسخهٔ تمیز
- رتبه‌بندی برترین‌ها تمام‌خودکار از دیتابیس؛ مداخلهٔ دستی یا پولی در ترتیب ممنوع
