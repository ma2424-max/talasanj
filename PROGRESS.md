# PROGRESS.md — دفترچهٔ پروژهٔ طلاسنج

## وضعیت کلی

- فاز فعلی: ۲
- آخرین اسپرینت کامل‌شده: S8
- نسخه: ۰٫۶٫۰
- تاریخ: ۱۴۰۵/۰۵/۲۵

## انجام‌شده

- [x] S0 — اسکلت Next.js، TypeScript strict، Tailwind و RTL
- [x] S1 — PostgreSQL/Neon + Drizzle + هفت جدول و seed
- [x] S2 — دیزاین سیستم و کامپوننت‌های مشترک
- [x] S3 — پروفایل پلتفرم و مسیر خروجی `/go/`
- [x] S4 — موتور امتیازدهی و ۷ تست
- [x] S5 — محاسبه‌گر هزینهٔ واقعی و ۶ تست
- [x] S6 — دایرکتوری، جستجو، مقایسه و برترین‌ها با ۶ تست
- [x] S7 — روش‌ها، سه راهنمای MDX، واژه‌نامه و ناوبری سراسری
- [x] S8 — `/funds/`، پنج ابزار جدید، sitemap، llms.txt، CI و راهنمای ویندوز

## کیفیت S8

- ۲۸ تست جدید: حداقل ۶ تست برای هر یک از چهار فرمول مالی + ۴ تست مقایسه‌گر
- مجموع تست‌های پروژه: ۴۷
- فرم ابزارها GET و بدون JavaScript قابل استفاده است
- نتایج پارامتردار noindex و canonical به URL تمیز دارند
- صندوق بدون دادهٔ رسمی EmptyState نشان می‌دهد و دادهٔ ساختگی ندارد

## قدم بعدی

- S9 — صفحات اعتماد (`about`، `contact`، `methodology`، `disclosure`، `privacy`، `terms`) + ثبت و تعدیل تجربهٔ کاربران + E2E.

## مسائل باز

- جدول `funds` فعلاً دادهٔ رسمی ندارد؛ EmptyState طبیعی است.
- صفحات اعتماد تا S9 هنوز ۴۰۴ هستند.
- نرخ مالیات در `assumptions.json` برابر `null` است؛ ابزارها بدون مالیات و همراه هشدار محاسبه می‌کنند تا نرخ رسمی منبع‌دار ثبت شود.

## تصمیم‌های قفل‌شده

- دادهٔ واقعی فقط با منبع و تاریخ؛ مقدار ناموجود «نامشخص» است.
- هر فرمول مالی حداقل ۵ تست طلایی دارد.
- ابزارها توصیهٔ سرمایه‌گذاری یا پیش‌بینی قیمت ارائه نمی‌دهند.
- اتصال دیتابیس فقط از `getDb()` و `DATABASE_URL` در `.env.local` است.
